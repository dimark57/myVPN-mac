# Tests (doc-6)

- `static-check.zsh` — статика без секретов (запускать из агента/CI локально).
- `fixtures/` — фейковые `.conf` (когда появятся для `sing-box check`).
- `expected/` — эталоны отчётов.
- Живой цикл `cycle-myvpn.zsh` — ещё не перенесён из Utilits; пока ручной QA + `myvpn status`.
